@using NackaPizzaOnline.Services
@model Cart
@inject DishIngredientService DishService

<h5>Varukorg</h5>

<table class="table">
    @foreach (var cartItem in Model.CartItems)
    {
        <tr>
            <th>
                @DishService.GetNameOfDish(cartItem.DishId)
            </th>
        </tr>
        //TODO Detta ska bli en rullgardin senare
        <tr>
            <td>
                <div>                    
                    @*<p>@DishService.WriteOutIngredients(Model.CartId, cartItem.CartItemId)</p><br />*@
                    <strong>Pris: @cartItem.Sum kr</strong>                    
                    <button class="btn btn-default">Ändra</button>
                    <button class="btn btn-danger">Ta bort</button>
                </div>
            </td>
        </tr>
                        }
</table>
<button class="btn btn-success">Betala</button>
